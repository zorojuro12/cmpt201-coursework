#include <stdio.h>
#include <unistd.h>
#include <sys/wait.h>

int main() {
  pid_t pid = fork();
  if (pid){
    printf("This is the parent process\n");
  }else {
    printf("This is the child process\n");
  }}
